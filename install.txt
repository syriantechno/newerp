# 🧱 الدخول إلى مجلد المشروع
cd path/to/your/project

# 🧩 تثبيت الحزم الخلفية (Laravel)
composer install

# 🎨 تثبيت الحزم الأمامية (لو في واجهة Vite أو Mix)
npm install

# ⚙️ نسخ ملف البيئة الافتراضي
cp .env.example .env

# 🗝️ توليد مفتاح التطبيق
php artisan key:generate

# 🗄️ إنشاء قاعدة البيانات (تأكد من اسمها بالـ .env)
php artisan migrate --seed

# 🚀 تشغيل السيرفر المحلي
php artisan serve

# 🎬 تشغيل واجهة التطوير (Vite أو Mix)
npm run dev
